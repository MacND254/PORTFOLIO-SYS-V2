# Set these in the portfolio-frontend Railway service's Variables tab.
# Replace portfolio-backend with the exact Railway backend service name if needed.
PORT=8080
BACKEND_HOST=${{portfolio-backend.RAILWAY_PRIVATE_DOMAIN}}:${{portfolio-backend.PORT}}
VITE_PLATFORM_DOMAIN=example.com
